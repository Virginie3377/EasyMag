{% extends '@EasyMagUser/BackOffice/Commercial/layout.html.twig' %}

{% block title %}Title{% endblock %}

{% block breadcrumb %}
    <h2>Clients</h2>
    <ol class="breadcrumb">
        <li>
            <a href="{{ path('commercial_index' ) }}">{{ "dashboard._"|trans }}</a>
        </li>
        <li class="active">
            <a href="{{ path('command_index' ) }}"><strong>Mes Commandes</strong></a>
        </li>
    </ol>
{% endblock %}

{% block content %}
<div class="row">
    <div class="col-lg-12">
        <div class="ibox float-e-margins">
            <div class="ibox-title">
                <h5>{{ "command._" |trans }}</h5>
            <div class="ibox-tools">
                <div class="btn-group">
                    <a href="{{ path('command_new') }}" class="btn btn-primary btn-xl">
                        <span class="{{ 'command.create'|trans }}"></span>
                        {{ 'command.create'|trans }}
                    </a>
                </div>
            </div>
        </div>
        <div class="ibox-content">
            <div class="table-responsive">
                {{ sg_datatables_render_html(datatable) }}
            </div>
        </div>
    </div>
</div>
</div>

   {# <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for command in commands %}
            <tr>
                <td><a href="{{ path('command_show', { 'id': command.id }) }}">{{ command.id }}</a></td>
                <td>{% if command.date %}{{ command.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ command.status }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('command_show', { 'id': command.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('command_edit', { 'id': command.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>#}

    <ul>
        <li>
            <a href="{{ path('command_new') }}">Create a new command</a>
        </li>
    </ul>
{% endblock %}

{% block javascript %}
{{ parent() }}
    {{ sg_datatables_render_js(datatable) }}
{% endblock javascript %}